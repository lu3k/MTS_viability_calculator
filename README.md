# MTS Viability Calculator 

A quick CLI and Webapp to transform the raw excel output of a Tecan Spark into a Viability % that can be copy-pasted into Prism. 

To run the CLI use python3 and pip install dependancies.txt
The usage is : 


To run the Webapp you need npm installed. 
run ´´´cd webapp && npm install && npm run dev´´´
